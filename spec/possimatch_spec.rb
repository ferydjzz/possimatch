require 'spec_helper'

describe Possimatch do
  it 'has a version number' do
    expect(Possimatch::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
